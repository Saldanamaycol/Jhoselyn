<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0, user-scalable=yes">
    <title>Para Jhoselyn 💖 | Un libro de amor</title>
    <link rel="stylesheet" href="style.css">
    <link href="https://fonts.googleapis.com/css2?family=Playfair+Display:wght@400;500;600;700;900&family=Quicksand:wght@300;400;500;600;700&display=swap" rel="stylesheet">
</head>
<body>
    <!-- Botón para modo noche/día -->
    <button class="theme-toggle" id="themeToggle" aria-label="Cambiar tema">🌙</button>

    <!-- Contenedor principal del libro -->
    <div class="book-container">
        <div class="book">
            
            <!-- Portada -->
            <div class="cover">
                <div class="cover-content">
                    <div class="heart-decoration">💖</div>
                    <h1>Para ti,</h1>
                    <h1 class="name-title">Jhoselyn</h1>
                    <div class="divider"></div>
                    <p class="subtitle">Un libro escrito con el corazón</p>
                    <div class="author">Con todo mi amor ✨</div>
                    <div class="flower">✿</div>
                </div>
                <div class="page-number">Abrir ▶</div>
            </div>

            <!-- Capítulo 1: Cuando te vi -->
            <div class="page">
                <div class="page-content">
                    <span class="chapter-badge">Capítulo 1</span>
                    <h2>Cuando te vi</h2>
                    <div class="heart-icon">💕</div>
                    <p>Dicen que uno recuerda el momento exacto en que la vida cambia para siempre. Yo recuerdo el tuyo.</p>
                    <p>No fue solo una sonrisa, fue la forma en que tu mirada hizo que el mundo dejara de girar por un segundo. En ese instante supe que no eras cualquier persona: eras <strong>mi persona</strong>.</p>
                    <p class="highlight">Jhoselyn, tu nombre se convirtió en mi palabra favorita antes de que pudiera pronunciarla.</p>
                    <div class="floating-hearts">❤️</div>
                </div>
                <div class="page-number">1</div>
            </div>

            <!-- Capítulo 2: El mapa de tu sonrisa -->
            <div class="page">
                <div class="page-content">
                    <span class="chapter-badge">Capítulo 2</span>
                    <h2>El mapa de tu sonrisa</h2>
                    <div class="heart-icon">😊💖</div>
                    <p>Si tuviera que dibujar la felicidad, dibujaría tu sonrisa. Es esa luz que ilumina hasta mis días grises.</p>
                    <p>Me encanta cómo tus ojos se achican cuando ríes de verdad, cómo tu risa se convierte en la melodía que quiero escuchar el resto de mi vida.</p>
                    <p>Contigo aprendí que la ternura no es un gesto enorme, sino la forma en que me miras cuando crees que no me doy cuenta.</p>
                </div>
                <div class="page-number">2</div>
            </div>

            <!-- Capítulo 3: Lo que me enseñaste -->
            <div class="page">
                <div class="page-content">
                    <span class="chapter-badge">Capítulo 3</span>
                    <h2>Lo que me enseñaste</h2>
                    <div class="heart-icon">🌹</div>
                    <p>Antes de ti, creía que sabía lo que era querer. Pero llegaste tú, Jhoselyn, y me enseñaste que el amor también es elegirnos todos los días.</p>
                    <p>Es reírnos sin motivo, es el silencio cómplice, es estar en la misma habitación haciendo cosas distintas pero sintiéndonos cerca.</p>
                    <p class="highlight">Gracias por enseñarme que las almas gemelas no se encuentran por casualidad: se reconocen.</p>
                </div>
                <div class="page-number">3</div>
            </div>

            <!-- Capítulo 4: Tus pequeños grandes detalles -->
            <div class="page">
                <div class="page-content">
                    <span class="chapter-badge">Capítulo 4</span>
                    <h2>Tus pequeños grandes detalles</h2>
                    <div class="heart-icon">✨</div>
                    <div class="love-list">
                        <p>💖 Amo la forma en que dices mi nombre, como si fuera un secreto.</p>
                        <p>💖 Amo cuando te duermes primero y dejas tu mano buscando la mía.</p>
                        <p>💖 Amo cuando me cuentas de tu día con esa emoción que solo tú tienes.</p>
                        <p>💖 Amo que seas fuerte, pero también que frente a mí te permitas ser vulnerable.</p>
                    </div>
                    <p>Eres un universo lleno de detalles, y mi mayor suerte es ser quien los descubre uno a uno.</p>
                </div>
                <div class="page-number">4</div>
            </div>

            <!-- Capítulo 5: Lo que veo cuando te veo -->
            <div class="page">
                <div class="page-content">
                    <span class="chapter-badge">Capítulo 5</span>
                    <h2>Lo que veo cuando te veo</h2>
                    <div class="heart-icon">👑</div>
                    <p>Cuando te veo, no solo veo a la mujer más hermosa. Veo a mi compañera de aventuras, a mi refugio en días de tormenta, a la razón por la que creo en el amor sin condiciones.</p>
                    <p>Veo ternura, fuerza, inteligencia y una bondad que pocas personas tienen.</p>
                    <p class="highlight">Jhoselyn, eres todo lo que está bien en mi vida.</p>
                </div>
                <div class="page-number">5</div>
            </div>

            <!-- Capítulo 6: Nuestro futuro en versos -->
            <div class="page">
                <div class="page-content">
                    <span class="chapter-badge">Capítulo 6</span>
                    <h2>Nuestro futuro en versos</h2>
                    <div class="heart-icon">🌅</div>
                    <div class="poem">
                        <p>Quiero seguir construyendo recuerdos contigo,</p>
                        <p>Desde los días aburridos que se vuelven especiales,</p>
                        <p>hasta los sueños grandes que hoy parecen lejanos,</p>
                        <p>pero que sé que lograremos si voy de tu mano.</p>
                        <br>
                        <p>Contigo quiero envejecer, reír, viajar,</p>
                        <p>crear un hogar lleno de risas,</p>
                        <p>y cada noche, antes de dormir, mirarte y decir:</p>
                        <p class="poem-highlight">qué suerte la mía, Jhoselyn.</p>
                    </div>
                </div>
                <div class="page-number">6</div>
            </div>

            <!-- Epílogo -->
            <div class="page">
                <div class="page-content">
                    <span class="chapter-badge">Epílogo</span>
                    <h2>Gracias</h2>
                    <div class="heart-icon">💌</div>
                    <p>Gracias por ser como eres. Por tu paciencia, por tu carácter, por tu forma de ver la vida. Gracias por elegirme cada día, por quedarte incluso cuando no es fácil.</p>
                    <p>Este libro virtual no tiene final, porque nuestra historia seguirá escribiéndose mientras tú me lo permitas.</p>
                    <div class="big-heart">❤️</div>
                    <p class="closing"><strong>Te amo, Jhoselyn. Eres lo más hermoso que me ha pasado.</strong></p>
                    <div class="signature">
                        <p>Con todo mi amor,</p>
                        <p class="signature-name">Tu compañero de alma</p>
                        <p>✨ 💖 ✨</p>
                    </div>
                </div>
                <div class="page-number">∞</div>
            </div>

            <!-- Contraportada -->
            <div class="back-cover">
                <div class="back-content">
                    <div class="flower-decoration">🌸</div>
                    <h3>Y esto apenas es</h3>
                    <h2>el primer capítulo</h2>
                    <p>de lo que quiero escribir contigo</p>
                    <div class="divider-small"></div>
                    <p class="final-message">Siempre tuya/o,</p>
                    <p class="final-name">por siempre</p>
                    <div class="hearts-row">💖 💕 💗 💓 💝</div>
                </div>
            </div>
        </div>

        <!-- Controles de navegación -->
        <div class="navigation">
            <button class="nav-btn prev-btn" id="prevBtn">◀ Anterior</button>
            <div class="page-indicator">
                <span id="currentPage">1</span> / <span id="totalPages">9</span>
            </div>
            <button class="nav-btn next-btn" id="nextBtn">Siguiente ▶</button>
        </div>
    </div>

    <!-- Mensaje flotante de amor (aparece al azar) -->
    <div class="floating-message" id="floatingMessage">💖</div>

    <script src="script.js"></script>
</body>
</html>
* {
    margin: 0;
    padding: 0;
    box-sizing: border-box;
}

:root {
    --bg-color: #fef5f0;
    --book-bg: #fffef7;
    --text-color: #4a2e2a;
    --accent-color: #e67e6b;
    --accent-light: #f9e0d9;
    --border-color: #f0d5cc;
    --shadow: rgba(0, 0, 0, 0.1);
    --heart-color: #e67e6b;
    --chapter-badge-bg: #f9e0d9;
}

body.dark {
    --bg-color: #1a1a2e;
    --book-bg: #2d2d44;
    --text-color: #f0e6e0;
    --accent-color: #ff9f8c;
    --accent-light: #3d3d5c;
    --border-color: #4a4a6a;
    --shadow: rgba(0, 0, 0, 0.3);
    --heart-color: #ff9f8c;
    --chapter-badge-bg: #3d3d5c;
}

body {
    font-family: 'Quicksand', sans-serif;
    background: var(--bg-color);
    min-height: 100vh;
    display: flex;
    justify-content: center;
    align-items: center;
    padding: 2rem;
    transition: background 0.3s ease;
}

/* Botón tema */
.theme-toggle {
    position: fixed;
    top: 20px;
    right: 20px;
    background: var(--accent-light);
    border: none;
    font-size: 1.5rem;
    cursor: pointer;
    padding: 10px;
    border-radius: 50%;
    width: 50px;
    height: 50px;
    display: flex;
    align-items: center;
    justify-content: center;
    transition: all 0.3s ease;
    box-shadow: 0 2px 10px var(--shadow);
    z-index: 100;
}

.theme-toggle:hover {
    transform: scale(1.1);
}

/* Contenedor del libro */
.book-container {
    max-width: 800px;
    width: 100%;
    margin: 0 auto;
}

.book {
    background: var(--book-bg);
    border-radius: 20px;
    box-shadow: 0 25px 50px -12px var(--shadow);
    overflow: hidden;
    position: relative;
    min-height: 600px;
    transition: background 0.3s ease;
}

/* Páginas */
.page, .cover, .back-cover {
    padding: 3rem 2.5rem;
    min-height: 600px;
    display: flex;
    flex-direction: column;
    justify-content: space-between;
    transition: all 0.3s ease;
}

/* Portada */
.cover {
    background: linear-gradient(135deg, #fceae8 0%, #ffd9d0 100%);
    text-align: center;
    justify-content: center;
    position: relative;
}

body.dark .cover {
    background: linear-gradient(135deg, #3d2a2a 0%, #5a3a3a 100%);
}

.cover-content {
    animation: fadeInUp 1s ease;
}

.heart-decoration {
    font-size: 4rem;
    animation: pulse 2s infinite;
}

.name-title {
    font-size: 3.5rem;
    font-family: 'Playfair Display', serif;
    color: var(--accent-color);
    margin: 0.5rem 0;
}

.cover h1 {
    font-size: 2.2rem;
    font-family: 'Playfair Display', serif;
    color: var(--text-color);
}

.divider {
    width: 80px;
    height: 2px;
    background: var(--accent-color);
    margin: 1.5rem auto;
}

.subtitle {
    font-size: 1.1rem;
    color: var(--text-color);
    opacity: 0.8;
    margin-bottom: 2rem;
}

.author {
    font-style: italic;
    color: var(--accent-color);
}

.flower {
    font-size: 2rem;
    margin-top: 2rem;
    opacity: 0.6;
}

/* Capítulos */
.chapter-badge {
    font-size: 0.8rem;
    text-transform: uppercase;
    letter-spacing: 2px;
    color: var(--accent-color);
    background: var(--chapter-badge-bg);
    padding: 0.3rem 1rem;
    border-radius: 20px;
    display: inline-block;
    margin-bottom: 1rem;
}

.page h2 {
    font-family: 'Playfair Display', serif;
    font-size: 2rem;
    color: var(--accent-color);
    margin-bottom: 1rem;
}

.heart-icon {
    font-size: 1.8rem;
    margin-bottom: 1.5rem;
    opacity: 0.7;
}

.page p {
    color: var(--text-color);
    line-height: 1.8;
    margin-bottom: 1rem;
    font-size: 1rem;
}

.highlight {
    font-size: 1.1rem;
    font-weight: 500;
    color: var(--accent-color);
    border-left: 3px solid var(--accent-color);
    padding-left: 1rem;
    margin: 1.5rem 0;
    font-style: italic;
}

/* Lista de detalles */
.love-list p {
    margin-bottom: 0.8rem;
    display: flex;
    align-items: center;
    gap: 0.5rem;
}

/* Poema */
.poem {
    text-align: center;
    font-style: italic;
    margin: 1rem 0;
}

.poem p {
    margin-bottom: 0.5rem;
}

.poem-highlight {
    font-size: 1.2rem;
    font-weight: bold;
    color: var(--accent-color);
    margin-top: 1rem;
}

/* Epílogo */
.big-heart {
    font-size: 3rem;
    text-align: center;
    margin: 1.5rem 0;
    animation: pulse 1.5s infinite;
}

.closing {
    text-align: center;
    font-size: 1.2rem;
    margin: 1.5rem 0;
}

.signature {
    text-align: center;
    margin-top: 2rem;
    font-style: italic;
}

.signature-name {
    font-family: 'Playfair Display', serif;
    font-size: 1.3rem;
    color: var(--accent-color);
    margin-top: 0.5rem;
}

/* Contraportada */
.back-cover {
    background: linear-gradient(135deg, #fceae8 0%, #ffd9d0 100%);
    text-align: center;
    justify-content: center;
}

body.dark .back-cover {
    background: linear-gradient(135deg, #3d2a2a 0%, #5a3a3a 100%);
}

.back-content h3 {
    font-size: 1.2rem;
    color: var(--text-color);
    font-weight: 400;
}

.back-content h2 {
    font-size: 2rem;
    font-family: 'Playfair Display', serif;
    color: var(--accent-color);
    margin: 0.5rem 0;
}

.back-content p {
    color: var(--text-color);
    margin: 1rem 0;
}

.divider-small {
    width: 50px;
    height: 1px;
    background: var(--accent-color);
    margin: 1.5rem auto;
}

.final-message {
    font-style: italic;
}

.final-name {
    font-family: 'Playfair Display', serif;
    font-size: 1.3rem;
    color: var(--accent-color);
    margin-bottom: 1rem;
}

.hearts-row {
    font-size: 1.5rem;
    letter-spacing: 5px;
    margin-top: 1.5rem;
}

/* Navegación */
.navigation {
    display: flex;
    justify-content: space-between;
    align-items: center;
    margin-top: 1.5rem;
    padding: 0 1rem;
}

.nav-btn {
    background: var(--accent-light);
    border: none;
    padding: 0.8rem 1.5rem;
    border-radius: 40px;
    font-family: 'Quicksand', sans-serif;
    font-weight: 600;
    color: var(--accent-color);
    cursor: pointer;
    transition: all 0.3s ease;
    font-size: 0.9rem;
}

.nav-btn:hover {
    background: var(--accent-color);
    color: white;
    transform: translateY(-2px);
}

.page-indicator {
    color: var(--text-color);
    font-size: 0.9rem;
    opacity: 0.7;
}

.page-number {
    text-align: right;
    font-size: 0.8rem;
    color: var(--text-color);
    opacity: 0.5;
    margin-top: 1rem;
}

/* Mensaje flotante */
.floating-message {
    position: fixed;
    bottom: 20px;
    left: 20px;
    font-size: 2rem;
    opacity: 0;
    transition: all 0.5s ease;
    pointer-events: none;
    z-index: 1000;
}

.floating-message.show {
    opacity: 1;
    transform: translateY(-20px);
}

/* Animaciones */
@keyframes fadeInUp {
    from {
        opacity: 0;
        transform: translateY(30px);
    }
    to {
        opacity: 1;
        transform: translateY(0);
    }
}

@keyframes pulse {
    0%, 100% {
        transform: scale(1);
    }
    50% {
        transform: scale(1.1);
    }
}

/* Responsive */
@media (max-width: 600px) {
    body {
        padding: 1rem;
    }
    
    .page, .cover, .back-cover {
        padding: 2rem 1.5rem;
        min-height: 500px;
    }
    
    .name-title {
        font-size: 2.5rem;
    }
    
    .cover h1 {
        font-size: 1.8rem;
    }
    
    .page h2 {
        font-size: 1.6rem;
    }
    
    .nav-btn {
        padding: 0.6rem 1.2rem;
        font-size: 0.8rem;
    }
}

/* Efecto de carga suave */
.page, .cover, .back-cover {
    display: none;
}

.page.active, .cover.active, .back-cover.active {
    display: flex;
}
// Estado actual
let currentIndex = 0;
let pages = [];

// Elementos del DOM
const prevBtn = document.getElementById('prevBtn');
const nextBtn = document.getElementById('nextBtn');
const currentPageSpan = document.getElementById('currentPage');
const totalPagesSpan = document.getElementById('totalPages');
const themeToggle = document.getElementById('themeToggle');
const floatingMessage = document.getElementById('floatingMessage');

// Mensajes románticos aleatorios
const loveMessages = [
    '💖 Te amo, Jhoselyn',
    '✨ Eres mi sueño hecho realidad',
    '🌹 Cada día te elijo a ti',
    '💫 Tu sonrisa ilumina mi mundo',
    '🌸 Eres hermosa por dentro y por fuera',
    '🌟 No hay nadie como tú',
    '💕 Gracias por existir',
    '🌺 Eres mi lugar favorito en el mundo',
    '💗 Contigo todo es mejor',
    '✨ Eres la mejor parte de mi día'
];

// Función para mostrar mensaje flotante aleatorio
function showRandomMessage() {
    const randomIndex = Math.floor(Math.random() * loveMessages.length);
    floatingMessage.textContent = loveMessages[randomIndex];
    floatingMessage.classList.add('show');
    
    setTimeout(() => {
        floatingMessage.classList.remove('show');
    }, 2000);
}

// Mostrar mensaje cada 30 segundos
setInterval(showRandomMessage, 30000);

// Mostrar uno al cargar la página
setTimeout(showRandomMessage, 3000);

// Inicializar páginas
function initPages() {
    pages = document.querySelectorAll('.page, .cover, .back-cover');
    totalPagesSpan.textContent = pages.length;
    
    // Ocultar todas y mostrar solo la primera
    pages.forEach((page, index) => {
        page.classList.remove('active');
        if (index === currentIndex) {
            page.classList.add('active');
        }
    });
    
    updatePageIndicator();
    updateButtons();
}

// Actualizar indicador de página
function updatePageIndicator() {
    if (pages[currentIndex]) {
        if (pages[currentIndex].classList.contains('cover')) {
            currentPageSpan.textContent = 'Portada';
        } else if (pages[currentIndex].classList.contains('back-cover')) {
            currentPageSpan.textContent = 'Contraportada';
        } else {
            currentPageSpan.textContent = currentIndex;
        }
    }
}

// Actualizar estado de botones
function updateButtons() {
    if (prevBtn && nextBtn) {
        prevBtn.disabled = currentIndex === 0;
        nextBtn.disabled = currentIndex === pages.length - 1;
        
        // Estilo visual para botones deshabilitados
        prevBtn.style.opacity = currentIndex === 0 ? '0.5' : '1';
        nextBtn.style.opacity = currentIndex === pages.length - 1 ? '0.5' : '1';
    }
}

// Cambiar de página
function goToPage(index) {
    if (index < 0 || index >= pages.length) return;
    
    // Quitar clase active de todas
    pages.forEach(page => {
        page.classList.remove('active');
    });
    
    // Agregar a la nueva página
    currentIndex = index;
    pages[currentIndex].classList.add('active');
    
    updatePageIndicator();
    updateButtons();
    
    // Efecto sutil al cambiar de página
    pages[currentIndex].style.animation = 'none';
    setTimeout(() => {
        if (pages[currentIndex]) {
            pages[currentIndex].style.animation = 'fadeInUp 0.5s ease';
        }
    }, 10);
}

// Navegación
function nextPage() {
    if (currentIndex < pages.length - 1) {
        goToPage(currentIndex + 1);
    }
}

function prevPage() {
    if (currentIndex > 0) {
        goToPage(currentIndex - 1);
    }
}

// Tema oscuro/claro
function initTheme() {
    const savedTheme = localStorage.getItem('theme');
    if (savedTheme === 'dark') {
        document.body.classList.add('dark');
        themeToggle.textContent = '☀️';
    } else {
        themeToggle.textContent = '🌙';
    }
}

function toggleTheme() {
    document.body.classList.toggle('dark');
    const isDark = document.body.classList.contains('dark');
    localStorage.setItem('theme', isDark ? 'dark' : 'light');
    themeToggle.textContent = isDark ? '☀️' : '🌙';
}

// Efecto de teclado (flechas izquierda/derecha)
function handleKeyPress(e) {
    if (e.key === 'ArrowRight') {
        nextPage();
    } else if (e.key === 'ArrowLeft') {
        prevPage();
    }
}

// Eventos de clic en las páginas (opcional: avanzar al hacer clic)
function initClickNavigation() {
    const book = document.querySelector('.book');
    if (book) {
        book.addEventListener('click', (e) => {
            // Evitar que se active si se hace clic en un botón
            if (e.target.closest('.nav-btn') || e.target.closest('.theme-toggle')) {
                return;
            }
            
            const rect = book.getBoundingClientRect();
            const clickX = e.clientX - rect.left;
            const bookWidth = rect.width;
            
            // Si se hace clic en la mitad derecha, avanzar; en la izquierda, retroceder
            if (clickX > bookWidth / 2) {
                nextPage();
            } else {
                prevPage();
            }
        });
    }
}

// Eventos táctiles para móviles
let touchStartX = 0;
let touchEndX = 0;

function handleTouchStart(e) {
    touchStartX = e.changedTouches[0].screenX;
}

function handleTouchEnd(e) {
    touchEndX = e.changedTouches[0].screenX;
    const swipeDistance = touchEndX - touchStartX;
    
    if (Math.abs(swipeDistance) > 50) {
        if (swipeDistance > 0) {
            prevPage();
        } else {
            nextPage();
        }
    }
}

// Efecto de corazones flotantes al hacer clic
function createFloatingHeart(e) {
    const heart = document.createElement('div');
    heart.innerHTML = '❤️';
    heart.style.position = 'fixed';
    heart.style.left = e.clientX + 'px';
    heart.style.top = e.clientY + 'px';
    heart.style.fontSize = '1.5rem';
    heart.style.pointerEvents = 'none';
    heart.style.zIndex = '9999';
    heart.style.opacity = '1';
    heart.style.transition = 'all 1s ease';
    document.body.appendChild(heart);
    
    setTimeout(() => {
        heart.style.transform = 'translateY(-100px)';
        heart.style.opacity = '0';
    }, 10);
    
    setTimeout(() => {
        heart.remove();
    }, 1000);
}

// Inicializar todo cuando el DOM esté listo
document.addEventListener('DOMContentLoaded', () => {
    initPages();
    initTheme();
    
    // Event listeners
    if (prevBtn) prevBtn.addEventListener('click', prevPage);
    if (nextBtn) nextBtn.addEventListener('click', nextPage);
    if (themeToggle) themeToggle.addEventListener('click', toggleTheme);
    
    document.addEventListener('keydown', handleKeyPress);
    
    // Navegación táctil
    const book = document.querySelector('.book');
    if (book) {
        book.addEventListener('touchstart', handleTouchStart);
        book.addEventListener('touchend', handleTouchEnd);
    }
    
    initClickNavigation();
    
    // Corazones al hacer clic en cualquier parte
    document.body.addEventListener('click', (e) => {
        // No crear corazón si se hace clic en botones
        if (!e.target.closest('.nav-btn') && !e.target.closest('.theme-toggle')) {
            createFloatingHeart(e);
        }
    });
});

// Guardar la página actual en caso de recarga (opcional)
window.addEventListener('beforeunload', () => {
    localStorage.setItem('lastPage', currentIndex);
});

// Recuperar última página (opcional)
window.addEventListener('load', () => {
    const lastPage = localStorage.getItem('lastPage');
    if (lastPage && parseInt(lastPage) !== currentIndex && pages[lastPage]) {
        goToPage(parseInt(lastPage));
    }
});
